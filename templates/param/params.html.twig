{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                {{ name }}
            </div>
        </div>
        <div class="row">
            <div class="col">
                {% if null != email %}
                    {{ email }}
                {% else %}
                    "No existeix l'email"
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
